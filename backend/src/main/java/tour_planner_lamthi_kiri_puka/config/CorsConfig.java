package tour_planner_lamthi_kiri_puka.config;

// CORS is now handled by SecurityConfig. This class is intentionally left empty.
// Keeping it so the config package is not empty.

import org.springframework.context.annotation.Configuration;

@Configuration
public class CorsConfig {
    // CORS configuration moved to SecurityConfig
}
